## 1. マイク／システム音声トリガーの視覚差分

- [ ] 1.1 `src/pages/audio/components/AudioSelection.tsx` のマイク用 `SelectTrigger` に、システム音声用と区別できる配色（既存デザイントークン、例: `primary` 系）をアイコン・枠線に適用する
- [ ] 1.2 同ファイルのシステム音声用 `SelectTrigger` に、マイク用と区別できる配色（既存デザイントークン、例: `secondary` 系）をアイコン・枠線に適用する
- [ ] 1.3 ライトモード・ダークモード両方でコントラスト比を目視確認し、必要に応じてテーマ変数を調整する

## 2. ドロップダウン項目のアイコン表示の確認・調整

- [ ] 2.1 マイク用 `SelectItem`（`MicIcon`）とシステム音声用 `SelectItem`（`HeadphonesIcon`）に、1で定めた配色を適用し、トリガーとの視覚的一貫性を持たせる

## 3. アクセシビリティラベル

- [ ] 3.1 マイク用 `SelectTrigger` に `aria-label="マイクデバイスを選択"`（相当のラベル）を追加する
- [ ] 3.2 システム音声用 `SelectTrigger` に `aria-label="システム音声デバイスを選択"`（相当のラベル）を追加する
- [ ] 3.3 各アイコンコンポーネントに `aria-hidden="true"` を設定し、スクリーンリーダーでの重複読み上げを防ぐ

## 4. スペック同期・動作確認

- [ ] 4.1 `openspec validate issue-5-device-dropdown-icons --strict` を実行しエラーがないことを確認する
- [ ] 4.2 `npm run typecheck` と `npm run lint` を実行しエラーがないことを確認する
- [ ] 4.3 アプリを起動し、オーディオ設定画面でマイク・システム音声のトリガーが配色・アイコンで見分けられることを確認する
- [ ] 4.4 各ドロップダウンを開き、項目のアイコン・配色が意図通り表示されることを確認する
- [ ] 4.5 スクリーンリーダー（またはアクセシビリティツリー確認）で、トリガーのラベルが種別を正しく読み上げることを確認する
