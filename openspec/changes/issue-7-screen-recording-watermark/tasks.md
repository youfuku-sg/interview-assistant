## 1. 未決事項の確定

- [ ] 1.1 ウォーターマークの文言・ロゴ・サイズ・不透明度・画面上の位置を決定する
- [ ] 1.2 設定オン/オフのデフォルト値と、ユーザーが自由にオフにできるかどうかを、要求仕様書 §8.6（倫理と利用方針）との整合性を踏まえて決定する
- [ ] 1.3 macOS 対応を本 MVP の範囲に含めるか、Windows 優先で先送りするかを決定する

## 2. バックエンド（Tauri/Rust）

- [ ] 2.1 `src-tauri/src/window.rs` にウォーターマーク用ウィンドウを生成する関数（`create_watermark_window` 等）を追加する。`contentProtected: false`、`alwaysOnTop`、`skipTaskbar`、クリックスルーを設定する
- [ ] 2.2 `src-tauri/src/lib.rs` のセットアップ処理からウォーターマークウィンドウの生成・表示を呼び出す
- [ ] 2.3 ウォーターマーク表示のオン/オフを切り替える Tauri コマンドを追加し、`lib.rs` に登録する
- [ ] 2.4 オン/オフ設定値をローカル設定ストレージ（`src-tauri/src/db/` または既存の設定機構）に永続化する

## 3. フロントエンド（React）

- [ ] 3.1 ウォーターマーク表示用のルート／コンポーネントを `src/routes/` 配下に追加する
- [ ] 3.2 設定画面（`src/pages/settings` 相当）にウォーターマーク表示オン/オフのトグル UI を追加する
- [ ] 3.3 トグル操作時にバックエンドのコマンドを呼び出し、永続化された設定値を反映する

## 4. 検証

- [ ] 4.1 Windows 上で画面共有ツール（Zoom/Teams 等）を使い、ウォーターマークが共有先に写ることを手動確認する
- [ ] 4.2 Windows 上で画面録画ソフトを使い、ウォーターマークが録画ファイルに写ることを手動確認する
- [ ] 4.3 メインオーバーレイ・ダッシュボードウィンドウの内容が、ウォーターマーク有効時も引き続き画面共有・録画に写らないことを確認する
- [ ] 4.4 ウォーターマーク表示中も、その下にある画面要素へのクリックが妨げられないことを確認する
- [ ] 4.5 設定のオン/オフ切り替えと、アプリ再起動後の設定維持を確認する
- [ ] 4.6 `npm run typecheck`、`npm run lint`、（Rust 変更時）`cargo fmt --check`・`cargo clippy` を実行する

## 5. ドキュメント

- [ ] 5.1 必要に応じて `docs/仕様/要求仕様書.md` §7.8（面接中オーバーレイ）にウォーターマーク機能を追記する
